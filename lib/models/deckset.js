/**
 * Represents a deck set which holds all the available card types of a game.
 */
export default class Deckset {
  /**
   * Creates a new deck set with specific cards available.
   * @param {number} cardSuitTypes The amount of different card suit types.
   * @param {number} cardNumberTypes The amount of different card number types.
   */
  constructor(cardSuitTypes = 4, cardNumberTypes = 13) {
    
  }
}
